`smerge-mode` - start mode

`C-c ^ n` - next conflict

`C-c ^ p` - previous conflict

`C-c ^ RET` - accept under cursor

`C-c ^ m` - accept  my commit

`C-c ^ o` - accept other than my commit

`C-c ^ a` - Keep all (Both ?)

`C-c ^ =` - chose '=' in menu to see pretty diff

`C-c ^ E` - ediff
