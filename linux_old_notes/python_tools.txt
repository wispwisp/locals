python -m pip instal virtualenv
python -m pip instal --user virtualenv      # -> /home/me/.local/bin/virtualenv

cd <dir>
virtualenv <env_name>

## virtualenv -p /usr/bin/python2.7 venv    # select if necessary

source .../<dir>/<env_name>/bin/activate

pip install <something>

deactivate

rm -rf <name>


#
pip freeze > requirements.txt
pip install -r requirements.txt
